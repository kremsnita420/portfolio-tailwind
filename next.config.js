const { i18n } = require('./next-i18next.config');

module.exports = {
    images: {
        domains: ['flowbite.com', 'images.unsplash.com'],
    },
    i18n: {
        defaultLocale: 'en',
        locales: ['en', 'sl'],
    },
}